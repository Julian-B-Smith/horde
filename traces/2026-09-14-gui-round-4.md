# 2026-09-14 — B123: asterisk line, morph box pinned under Presets, panic leaves the bar, copy from the other OSC

**Asterisk.** `#mcorners .row { grid-template-columns:64px auto minmax(0,1fr) 12px }`.

**Morph box.** `.ctlcol` is `display:block; columns:240px` (ADR-098 tiling) —
column balancing moved `#morphMiniBox` whenever the Bend cluster's
`data-when` rows changed during a morph. The box now follows the Presets
cluster in DOM order (the column's first items are stable).

**Panic.** `#panicBtn` removed from `.tabs`; the SET page button carries the
full title; Escape (ADR-032) unchanged.

**Copy from OSC.** `PARAM_MENU` gains `copyParam` / `copySection` (per-osc,
non-fixed params, `numOsc > 1`): value = `lastParams[base + otherOsc*1000]`;
writes bracketed by one gesture on the clicked id so history gets one node.
`sectionIds(row)` = the cluster's per-osc `[data-p]` ids for the edited osc.

**Oracle.** `lab_load_check` GREEN; `./verify fast` exit 0 (GUI-only).
