# 2026-09-14 — B120: visualizer resolution + refresh, rail light-mode tokens

**Asks (human).** Higher resolution and refresh for the visualizers (not the
specimen); the rail's light-mode colours.

**Resolution.** `src/gui/gui2.html`: `VS()` / `fitViz()`; `paintScopeInto`,
`_drawSpecOne`, `paintCarpet` and `scan()` scale their strokes by `VS()`;
CSS pins `canvas.wavscope` 80px, `canvas.spec` 70px, `#specC2` 256×58,
`canvas.carpetM` 100%×58, `#carpetC` 256×58. Pane measurement: wave box
538×77 → bitmap 1076×154 at dpr 2; spec 1076×134; carpet 1076×110; boxes
unchanged.

**Refresh.** The plugin-path fetch divisor is `fetchEma < 6 ? 1 : < 12 ? 2 : 3`
(was a fixed 3 = 20 Hz, B76). Paints happen on receipt, so a cheap bridge
paints at rAF rate. Host-delivered: the human reads the footer.

**Spectrum.** `src/gui/hypersaw_gui_common.h`: `kBins` 256 → 512 in both
`hzGetSpec` and `hzFrame`; `computeSpectrum` maps any n; the GUI reads
`bins.length`.

**Rail, light mode.** off-spine lanes `--t2`; `body:not(.dark) .histnode.ahead`
uses `--t2` on `--panel2`.

**Oracle.** `lab_load_check` GREEN; `./verify fast` exit 0; `./verify full`
in the PR body.

**Addendum 2026-09-14 (dark mode).** The human's actual report was the DARK rail: "the central strands are in black, which is nearly invisible". Cause: the rail read tokens from the document root, and the dark palette is set on `body.dark`, so it painted the cream palette's ink. Now reads through `TOK()` (body-resolved, cache cleared on scheme switch). Pane check in dark: spine stroke resolves to #EFE9FA.
