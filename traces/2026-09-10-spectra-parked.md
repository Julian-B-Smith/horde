# 2026-09-10 — SPECTRA stays parked: the lead's "expose" recommendation retracted

**What changed.** The 1.0 roster had "SPECTRA exposed or removed (lead recommends
EXPOSE)" — a regression of the human's standing ruling (ROADMAP §"SPECTRA
PARKED", 2026-08-18; restated 2026-09-10). Corrected in the roster, the parked
list and the sequencing line; PARKED.md entry added with an explicit revisit
trigger (only the human); a STATUS header on `src/spectra_core.h` so no audit
framing re-opens it; CLAUDE.md §Domain one-liner; REFLECTIONS entry (candidate
library lesson). Engine param 43 keeps its frozen id and stays unexposed.

**Verify.** `./verify fast` — docs + a comment header; spectra_check unaffected.
