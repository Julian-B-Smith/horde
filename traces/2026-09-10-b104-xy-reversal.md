# 2026-09-10 — B104: per-osc XY pads direct, MAIN pad the one macro (ADR-156)

**What changed.** Shell: detune/K defaults back to the lab's 0.28 / 0 (the
2026-08-30 floors reversed); modInstallDefaults() and the birth-time floor
applies deleted; alias sources 10-13 read 0 (retired, MOD_SRC_NAMES says so);
174-177 stored but drive nothing. GUI: the OSC pad writes effId(4)/effId(6)
directly through a per-axis value map (x 0..1 detune, y -1..1 K); MAIN pad
unchanged (macro pair via 179/180); osc-page assignment selects and the ASN
mirror deleted; SET's XY-assignment cluster kept (gui_reach) with a retired
note; route notes state the direct mapping. Oracles aligned to ADR-156:
state_check expects det 0.28; paramscope's macro pins kept as inert
assumption-statements with the comment corrected.

**Evidence.** In-pane: pad quarter-point wrote detune 0.25 / K 0.5, corner
wrote 1 / -1, MAIN pad still writes macros. `./verify full` exit 0 — parity
156/156 without re-baseline (scenarios set detune/K explicitly; parity drives
the cores, not the table); lab_load 26/0; gen_gui_controls regenerated for
the new defaults.
